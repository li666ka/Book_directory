export const connectionConfig: object = {
	host: 'localhost',
	port: '8080',
	user: 'root',
	database: 'booklist',
	password: 'mypassword',
};

export const CONTENT_ROOT: string = 'content';

export default { connectionConfig, CONTENT_ROOT };
